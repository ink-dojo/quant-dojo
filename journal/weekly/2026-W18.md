# 周报：2026-W18（2026-04-27 ~ 2026-05-01）

## 2026-04-28 — spec v4 否决与项目收窄

### 做了什么
- 记录 Jialong 对 `paper_trade_spec_v4` 的最终否决：RIAD + DSR#30 BB-only 50/50 不 go-live，不实现 `pipeline/riad_signal.py`。
- 同步更新 `CLAUDE.md` / `README.md` / `README.zh-CN.md` / `ROADMAP.md` / `TODO.md` / `ALPHA_THEORY_2026.md`，把项目状态从“待批准候选 / Phase 8 推进”改为“暂无 go-live candidate / Phase 8 暂缓”。
- 更新 portfolio 文案，把 RIAD combo 从“blocked candidate”明确成 “rejected / no go-live candidate”。

### 发现与判断
- spec v4 的 headline SR 不能覆盖两个核心问题：DSR 0.920 依赖一次性例外，且 RIAD 可执行股票池 / Fold 3 regime 表现没有保住 baseline 结果。
- 近期继续铺 iCloud / 存储迁移 / 宽泛 infra 会稀释主线；这些事项先不处理，除非直接阻塞研究复盘。

### 下一步
- 只做 RIAD Fold 3 / regime shift 诊断，不追加参数 sweep。
- 清理 open issues：关闭过期研究任务，或标记暂缓。
- portfolio 继续保持“研究档案”定位，不包装成当前交易策略。

## 2026-04-29 — 引入 alphalens-reloaded 作为因子报告层（Issue #49）

### 做了什么
- 确认 quant-dojo 当前哲学是「快速否决弱策略」，缺一个**外部独立报告层**做对照。
- 调研后选 stefan-jansen/alphalens-reloaded 0.4.6（pandas 2.x 原生，615 commits 维护，pip 一行装）。
- 完成全套接入：
  - `pyproject.toml` extras `[research]` 加 `alphalens-reloaded>=0.4.6`，不污染主依赖（`pip install -e .[research]` 才装）
  - `utils/alphalens_adapter.py`：宽表 → MultiIndex Series 转换 + `consistency_check_ic` 红线（本地 IC ≈ alphalens IC，差 < 5e-5）
  - `tests/test_alphalens_adapter.py`：6 个测试覆盖 fwd_period 1/5/10 三个前瞻周期 + 格式校验，全过
  - `research/alphalens_audits/_runner.py`：通用 audit runner，调 `alphalens.plotting` 子函数（`tears.create_*` 内部 `plt.show()` 在 Agg backend 抓不到 figure），保存 IC / Returns / Turnover 三类 ~16 张 PNG + summary.json
  - 三个因子 audit 脚本各跑一次

### 发现与判断
**关键修正**：原 issue body 的目标因子 polar_pv / mda_drift / DSR #30 部分不适配 alphalens：
- `polar_pv_factor/` 在 CLAUDE.md「禁止修改」清单，且因子定义在 notebook 用的是独立 exam_data，不重组成本太高 → 撤
- `DSR #30` 是事件驱动 P&L 时间序列（`net_return` 单列），不是 cross-sectional factor，alphalens 在设计上不适用 → 撤
- `mda_drift_scores` 是 (fiscal_year × symbol) 年频稀疏因子（7 × 500），需要 (a) publish_date 对齐到下一交易日 (b) ffill 到日度才能让 alphalens quintile 切；且 alphalens 内部 `pct_change(fill_method='pad')` 与本地 fill_method=None 在稀疏数据上不可对齐，加 `skip_consistency=True` 通道处理。

替换为三个 cross-sectional 干净因子的 audit：

| 因子 | 频率 | shape | IC 1D/5D/10D/20D（PEAD）或 20D/60D/120D/250D（mda） | ICIR | 结论 |
|---|---|---|---|---|---|
| `surprise_yoy_z`（PEAD 业绩超预期）| daily | 2919×5477 | +0.006 / +0.013 / +0.017 / +0.020 | 0.07 ~ 0.20 | 弱正向，中长期渐显著 |
| `insider_net_buying_60d_ind_size_neu`（行业市值中性化版）| daily | 1942×5477 | -0.004 / -0.006 / -0.007 / -0.008 | -0.07 ~ -0.13 | 中性化后信号死掉，正向版 87% 是 0 不能切 quintile |
| `mda_drift`（年报 MD&A 漂移）| annual→ffilled | 1420×500 | -0.002 / -0.003 / -0.011 / -0.022 | -0.02 ~ -0.38 | **250D ICIR -0.38 通过稳定门槛**，方向反向（drift 大 = 未来弱） |

### 下一步
- 给 jialong 看 mda_drift 250D 反向显著的结果，决定是否转向「负 score 选股」的反向策略
- Issue #48 qlib baseline（W19 开始）作为外部独立基线对照 DSR #30 BB 主板候选
- 如果未来再做新因子，直接复用 `research/alphalens_audits/_runner.py`，5 行就能出 tear sheet

## 2026-04-29（同日续）— Issue #48 qlib baseline 三阶段连续完成

提前把 W19 的 qlib baseline 也压缩到本周完成（用户要求"连续做 3 周不停"）。

### Phase A — 环境与数据骨架
- `research/qlib_baseline/.venv` 独立 venv（不污染主项目）
- `setup_env.sh` 一键装 pyqlib + lightgbm + brew libomp + cn_data v3
- `verify_data.py` 自检通过含 PIT-correctness 断言（universe 必须有 late_join + early_kick）

**重要发现 1**：qlib v3 cn_data PIT universe 实际只更新到 **2020-09-25**（calendar 是 2022-12-30），之后退化成 snapshot。所以 baseline test 不能超过 2020-09-25。

**修正**：原 issue body 计划写 `tushare_to_qlib_bin.py`，发现 tushare 缓存只有 `daily_basic.close`（pro.daily 接口被吊销，详见 `neutralize_and_cost.py:76`），Alpha158 大量特征要全 OHLCV。改用 qlib 官方 cn_data（业界标准 baseline 本就用这套），更标准。

### Phase B — 跑通 Alpha158 + LightGBM
`run_baseline.py` 完整 workflow：handler → dataset → LightGBM fit → predict → TopkDropout backtest → risk_analysis。

时间切分：train 2008-2014 / valid 2015-2016 / test 2017-2020.09.25（受 PIT 限制）。
交易成本：单边 0.15%（对齐 CLAUDE.md 红线，覆盖 qlib 默认 0.0005/0.0015）。
LightGBM 参数：复刻 qlib 官方 `workflow_config_lightgbm_Alpha158.yaml`（lr=0.0421 / num_leaves=210 等）+ seed=2026 deterministic=True。

baseline 结果（run 20260429_223928, 911 days）：
- excess **without** cost: 年化 **10.67%**, IR 1.16, MaxDD -8.3%
- excess **with** cost: 年化 **3.75%**, IR 0.41, MaxDD -9.7%
- 成本一刀砍掉 ~7pct/年化（A 股 cross-sectional ML 对换手敏感的本质）

### Phase C — 对照 DSR #30 + 决策

`compare_dsr30.py` 把 qlib baseline 与 DSR #30 BB 主板 rescaled / ensemble 在重叠期 2018-01-02 ~ 2020-09-25（667 days, 2.5y）对齐，同 cost basis 同 benchmark 算指标。

**关键发现 2 — 对照表**：

| 候选 | 年化 net | net Sharpe | excess vs CSI300 | MaxDD |
|---|---:|---:|---:|---:|
| qlib Alpha158-LightGBM | 7.74% | 0.34 | 3.08% | -28.6% |
| CSI300 benchmark | 4.86% | 0.33 | — | -32.5% |
| **DSR #30 BB 主板 rescaled** | **14.62%** | **0.89** | **6.26%** | **-17.5%** |
| DSR #30 ensemble | 8.63% | 0.49 | 1.21% | -18.3% |

**判决性证据**：
1. DSR #30 BB 单脚 net Sharpe 0.89（utils.metrics rf=0.02 ddof=1 标准口径）通过 CLAUDE.md 门槛 > 0.8，年化 14.62% 接近 15%，MaxDD -17.5% 远低于 30% —— **点估计有 edge**（~2.6x baseline），但 SE ≈ 0.74 与 baseline 差不到 1σ，统计上不构成翻案 Phase 3 CI_low 0.20 fail
2. ensemble 反而比 BB 单脚显著弱（excess 1.21% vs 6.26%）—— **印证 jialong 2026-04-28 否决 spec v4 RIAD+DSR#30 50/50 的逻辑**：50/50 摊薄了 BB edge
3. 重叠期只有 2.5y < CLAUDE.md 3y 红线 —— 这次对照只能作**参考性外部基线**，不能单独触发 go-live

详细决策建议见 `journal/qlib_baseline_dsr30_compare_20260429.md`。

### 下一步建议
- jialong 选 DSR #30 4/5 候选 option A/B/C，缓解 CI_low 0.20 fail 那一项
- 完整 2018-2026 区间（不受 qlib 数据限制）复跑 BB 单脚，凑齐 3y+ 红线
- 评估 capacity / live-vs-backtest 后再决定 paper-trade
- 不再考虑 BB+PV ensemble / 50-50 双腿配置

## 2026-04-29（同日续 2）— Issue #54 skfolio 接入

接入 [skfolio](https://github.com/skfolio/skfolio) 0.20.1 作 [research] extras 可选依赖，新增两类能力（不替换现有红线）：

### CombinatorialPurgedCV (AFML Ch.12)
- `utils/purged_cv.py` 加 `combinatorial_purged_cv_splits()` thin wrapper
- 与现有 `purged_kfold_indices` 不是 1-1 替换 —— PurgedKFold 是 k 折每折 1 test 段；CPCV 是 C(n, k) 条 backtest paths（n_folds=10, n_test_folds=2 → 45 paths），提供 Sharpe 的 sampling distribution
- 现有 `purged_kfold_indices` 保留作 reference + 简单场景

### 组合优化 backend
- `utils/position_sizing.py` 加 `skfolio_optimizer(method='hrp'|'min_cvar'|'max_div')`
- 不动 `equal_weight` / `risk_parity` / `max_single_stock` 现有红线接口
- 未来 BB 单脚 paper-trade 升级到多策略组合时可用

### 测试
`tests/test_skfolio_integration.py` 18/18 过：
- CPCV path 数 / train-test 不重叠 / purge 生效
- HRP/MinCVaR/MaxDiv 权重和=1 / 非负 / 不退化 / max_weight 约束 / 缺失 symbol 跳过 / 短历史降级

### 发现
- `HierarchicalRiskParity.max_weights` 在 skfolio 0.20 不严格 cap（现象级 hint，硬约束要用 `MeanRisk.max_weights`）；测试只验 `MeanRisk(min_cvar)` 的 cap 行为
- skfolio CPCV 的 test_idx 是 `list[np.ndarray]`（每个 path 由 n_test_folds 个不连续段组成），不是单 array — 用前必须 concat

## 2026-04-30 — paper-trade cron 静默故障 + 老 clone 余孽根因清理

### 事件
- 2026-04-14 标记"已清理"的两份 clone 实际并没真清掉：`/Users/karan/projects/quant-dojo` 还在，pip editable install 仍指向那里（`Editable project location: /Users/karan/projects/quant-dojo`）。
- 后果：cron 从 `paper_trade_cron.log` 看出已经**至少自 2026-04-24 起静默 fail**，每天报 `FileNotFoundError: /Users/karan/projects/quant-dojo/data/raw/events/_all_buyback.parquet`（老 clone 没 SSD 数据 link）。`live/portfolio/nav.csv` 最新 row 停在 2026-04-24，`trades.json` 119 笔不动；前端 `state.json` 因为内容没变化也没 push，看上去"还在跑"实际是僵尸。
- 根因：editable install 没跟着 clone 迁移，`pipeline.event_signal` 解析到老仓库下的同名文件。journal 路径不是硬编码——是 `sys.path` resolve 错。

### 修复
1. `pip uninstall quant-dojo -y && pip install -e .` 在工作目录重装（`Editable project location` 重指 `/Users/karan/work/quant-dojo`）
2. `rm -rf /Users/karan/projects/quant-dojo`（独立 untracked 文件 = 1.3KB 旧 W18 周报 snapshot, 已含在工作 clone）；连带 rmdir 空 `~/projects`
3. 重跑 `scripts/paper_trade_cron.sh` → exit=0, "2026-05-01 不是交易日 (劳动节假期)" skip 正常

### 教训
- "已清理两份 clone" 的 memory 实际只清了**意识**没清磁盘。下次清 clone 必须连 `pip show <pkg> | grep Editable` 一起验。
- cron 静默 fail 6 天没人发现，是因为 step 4 (`state.json` 未变化跳过 push) 把 step 3 (`paper_trade_daily exit=1`) 的失败掩盖了。后续考虑 cron failure → 写一个 alert 文件让 portfolio 前端能 surface "stale data" 状态。

### 状态
- ✅ editable install fixed
- ✅ 老 clone 删除
- ✅ cron pipeline 恢复（exit=0）
- ⏭️  下个交易日 (5/6) 自然触发首个真实 EOD step 验证
- ⚠️  BB event paper trader 继续跑：spec v4 否决后定位 = "外部 baseline observability 数据收集"，不是 Tier 1+ 候选，但 nav/trades 连续记录有价值
- ⚠️  roe_stability Tier 0 paper smoke 是另起 signal-only 流（cross-sectional vs 现 EventPaperTrader 事件驱动，不能直接共享 trade logic），30 天数据收齐后再评估是否 substantial refactor 接进统一 PaperTrader
