# 2026-W17 (04-20 ~ 04-26) Weekly journal

## 2026-04-22 周三 — paper-trade 切换 BB-only (spec v3)

### 做了什么

1. **DSR #31 (insider ggcg) 数据可行性复核**
   - `data/raw/events/_all_ggcg_2018_2025.parquet` (5MB, 2026-04-18 akshare 下载) 已在本地, 8,842 增持事件 (0.1%-20% 过滤后), 覆盖 2018-2025 各年均匀
   - 复跑 `dsr31_three_way_ensemble.py`: insider-only **0/5** (ann 5.83%, SR 0.30, MDD -46.1%), 3-way ensemble **2/5**
   - 关键: insider 2018 ann -45.8% SR -2.68 是真实尾部风险 (大股东底部扛旗不稳), 和 8-yr 8,842 事件大样本共同说明 alpha 不足, 不是数据量问题
   - 结论: C 路径数据拿得到但 alpha 证伪, 不采纳

2. **锁定 spec v3 BB-only pre-reg**
   - 写 `journal/paper_trade_spec_v3_bb_only_20260422.md` (supersedes v2)
   - 关键数字全部锁从 `portfolio/public/data/event_driven/dsr30_standalone_wf.json` 取,
     不再 freestyle:
     - ann 15.99%, SR 0.84, MDD -29.68%, PSR 0.996, CI [0.20, 1.50]
     - WF n=12: median SR 0.73 / Q25 0.39 (PASS)
     - Regime: bull 1.37 / bear 0.01 / sideways 0.77 → 2/3 PASS (bear fail)
     - Trade-level (684 trades): win-rate **69.7%**, top-5 9.4% (PASS)
     - **生产级 3/4** (single-sample 5-gate 仅 CI_low fail)
   - Phase 1 initial capital 5%; 3mo 升 15%; 6mo 升 50% (与 v2 相同时点但阶梯收紧)

3. **代码 refactor: ensemble_mix + legs_enabled 变成 config 驱动**
   - `pipeline/event_signal.py::generate_daily_signal(..., legs_enabled: dict | None)`: None = 两腿都开 (v2 行为); {bb:True, pv:False} = BB-only
   - `live/event_paper_trader.py::EventPaperTrader(..., ensemble_mix: dict | None)`: None = {bb:0.5, pv:0.5}; v3 传 {bb:1.0, pv:0.0}
   - `_compute_target_weights` 内部用 `self.ensemble_mix["bb"]/"pv"` 替代模块常量 ENSEMBLE_MIX
   - `scripts/paper_trade_daily.py::_load_config()` 读 `paper_trade/config.json`, 把 legs_enabled + ensemble_mix 传给 signal + trader
   - 默认行为不变 (config 缺失时 fallback 到 v2 50/50), 向后兼容

4. **config.json 切到 BB-only**
   - `spec_version: "v3"`, `strategy_id: "dsr30_bb_mainboard_rescaled_bbonly"`
   - `legs_enabled: {bb: true, pv: false}`, `ensemble_mix: {bb: 1.0, pv: 0.0}`
   - `initial_capital_pct_of_total: 0.05` (保留), 其他参数不变

5. **测试新增 + 全部通过**
   - `test_trader_bb_only_mode_doubles_weight`: 1.0×BB_UNIT ≈ 0.1323 而非 0.5×
   - `test_trader_bb_only_ignores_pv_entries`: PV entry 即使传入也 weight 0, 不买
   - `test_signal_respects_legs_enabled_pv_off`: PV 候选/admitted 都被强制清零
   - 全套 49 test PASS (event_signal 17 + event_paper_trader 15 + event_kill_switch 11 + paper_trade_daily 6)

6. **Smoke test BB-only 8yr vs backtest parquet**
   - `python scripts/paper_trade_smoke_test.py --mode strict --legs bb`
   - mean abs daily delta **1.77 bps** (tolerance 10 bps, 实测甚至优于 spec 目标 5 bps)
   - corr(sim, bt) **0.9994**; sim ann 15.38% vs bt ann 16.00% (62 bps/yr drift, 整数股 + cash drift 合理)
   - sim SR 0.92 vs bt SR 0.95

### 发现了什么

- **v2 spec §9 item 3 自己留的 out ("若 BB 单腿优于 ensemble 则独立 pre-reg BB-only") 真的触发了** — pre-reg discipline 起作用, 不是事后改口径
- **PV 腿是稀释剂不是分散器**: BB 4/5 + PV 1/5 → ensemble 2/5. correlation matrix 没体现 ensemble dilution, 只有 full-stack 5-gate 才能看出
- **bootstrap CI_low 对样本 variance 非常敏感**: 8-yr BB SR 0.84 (ann 16% / std 19%), bootstrap std(SR) ≈ 0.32 → CI_low 直接掉到 0.20. 要过 0.5 需要 live SR 稳定 ≥ 1.0 超过 6 mo
- **Insider 2018 tail risk**: 单年 ann -45.8%, 没发现很多 drawdown control 能救 — alpha 本身薄
- **config-driven 架构优于硬编码**: 之前 ENSEMBLE_MIX = 0.5 是 module-level 常量, 如果 v2/v3 共存期 (e.g. 不同 portfolio_dir 跑不同 spec) 会冲突; 现在 trader 构造时注入, 一个 process 可同时有 v2 / v3 两个实例

### 下一步

- [ ] 下个 sprint 研究 orthogonal alpha (非 PV / insider, e.g. 股权激励 / 股东大会 / 解禁)
  — 若单腿 4/5 且 corr(BB, X) < 0.3 → 可启动新 pre-reg 做 2-腿 ensemble, CI_low 有望过 0.5

---

## 2026-04-22 周三 (深夜) — BB-only go-live ✅

jialong 拍板今天上线. 本地时间 00:30 AM (SH 12:30 周三) 做完所有基础设施切换.

### 做了什么

1. **启用 config**: `enabled=true, phase=live_phase1, started_at=2026-04-17` (tushare token 过期,
   价格数据最新只到 4-17 周五, 以 4-17 为 go-live 锚点; token 修后 backfill 4-18/4-20/4-21/4-22)
2. **non-trading day 优雅跳过**: `paper_trade_daily.py` 不再 raise ValueError, 改 exit 0 + log
3. **portfolio state 快照 JSON**: 每次 EOD 末尾额外写 `portfolio/public/data/paper_trade/state.json`
   (spec/phase/NAV/positions/trades/kill 全包), 驱动可视化
4. **/live/paper-trade 页面**: `portfolio/src/app/live/paper-trade/page.tsx` + `NavCurve.tsx`
   客户端组件, 从 state.json 读, 显示 8 指标 MetricGrid + NAV 曲线 + 今日 trades + 活仓表 +
   kill 详情 + config 摘要. TS 类型 `PaperTradeState` 加在 `portfolio/src/lib/types.ts`. tsc noEmit 过
5. **launchd 调度**:
   - `dev.quantdojo.paper-trade-daily` — Mon-Fri 本地 09:30 = SH 21:30 (EOD 数据肯定有)
   - `dev.quantdojo.paper-trade-monthly` — 每月 1 号本地 10:00 跑 monthly review
   - 两个 plist bootstrap 成功, `launchctl list | grep quantdojo` 两行都在
   - 机器睡眠时 launchd 不触发 → README 补了手动 backfill 指令
6. **macOS 本地通知**: `_push_alert()` 里加 osascript display notification, kill action != OK
   时本地弹通知 (无需外部 webhook, future 再接 Slack/邮件)
7. **wrapper shell**: `scripts/paper_trade_cron.sh` 先 daily_update, 失败不 abort, 再 paper_trade_daily.
   日志 → `logs/paper_trade_cron.log`
8. **首次 live run** 2026-04-17 (5-trading-days-ago): kill OK, running_days=1, 0 new entries
   (主板 4-16 无合格 BB 事件, 正常), NAV=1,000,000

### 发现了什么

- **tushare token 过期**: `.env` 里 token 返回 "您的token不对". 这是外部依赖. 需要 jialong 到
  tushare.pro 续 token 后填回 `.env`. paper-trade 链路本身能跑, 只是数据会停在 4-17.
- **live 数据 latency**: 如果 token 修复 + 每天 09:30 触发, 实际上看到的是上一日 EOD 数据.
  对 paper-trade 足够 (真实 T+1 open 成交语义).
- **portfolio 现有 live 页是 cross-sectional 策略的 live dashboard**. DSR #30 event-driven
  应该走独立 sub-route `/live/paper-trade` 不搅原页. 未来 live 页面应加入口卡.

### 下一步 (go-live 之后)

- [ ] jialong 补 tushare token → backfill 2026-04-18/04-20/04-21/04-22 (4 个交易日)
- [ ] 观察 1 周实盘数据 (running_days 5-6 以后会开始有 signal)
- [ ] portfolio 网页 Vercel 部署后肉眼看 paper-trade 页渲染
- [ ] 下个 sprint 研究 orthogonal alpha (股权激励 / 股东大会 / 解禁) 准备 DSR #32

---

## 2026-04-22 周三 (凌晨) — 差异化因子挖掘 (RIAD + MFD)

Issue #33. 依用户要求重点看 2025 年数据, 尝试 2 个 A 股独有因子.

### 做了什么

1. **数据审计**: 基于 `journal/tushare_data_inventory_20260420.md`,
   确认 `ths_hot` / `dc_hot` / `stk_surv` / `moneyflow` 等未被已有 ~20 因子利用.
2. **RIAD 因子** (Retail-Institution Attention Divergence, `research/factors/retail_inst_divergence/`):
   - 散户热度 (ths_hot + dc_hot 榜单 rank 加权) 减去 机构调研机构数 (log1p).
   - cross-section zscore 相减. window: retail 20d, inst 60d.
   - FULL IC=-0.070, ICIR=-0.89, HAC t=-5.16
   - **OOS 2025: IC=-0.059, ICIR=-1.25, HAC t=-13.30, pct_pos=8.9%**
   - size-neutralize 后 corr=0.996 且 IC 略增强 → 不是小盘代理
   - 分层 OOS Q1-Q5 差 +1.09% (每 20 日持有)
3. **MFD 因子** (MoneyFlow Divergence, `research/factors/moneyflow_divergence/`):
   - elg (超大单) 净流入 ratio 减去 sm (小单) 净流入 ratio. 归一化除以总成交额.
   - FULL IC=-0.020, HAC t=-4.28, pct_pos=36.6% → **反转因子**
   - 解读: elg 不等于 smart money, 很可能是尾盘对倒 / 派发假象
4. **RIAD+MFD 合成**: IC=+0.069, 与 RIAD 单独 0.070 几乎无差别 → MFD 无增量 alpha
5. **FMD 设计待命**: `research/factors/foreign_margin_divergence/factor.py` 写好, 但
   `northbound/000001.parquet` 2025 仅 5 行, 数据缺失, 暂不能用. 待 tushare 补齐或换接口.

### 发现了什么

- **RIAD 是一个真实、稳健、A 股独有、2025 有效的 cross-sectional alpha**:
  - 非 size 代理 (相关度 0.996 但 IC 略增强)
  - 主要 alpha 在做空端 (散户追捧股跑输), IS Q5=-1.10%/期, OOS Q5=+2.00% vs Q1=+3.09%
  - ICIR ≥ 0.85, 方向高度一致 (周 IC 只有 9% 是正的)
- **"大单净流入 = bullish"是错的**: tushare elg 分类有噪音, 但反过来 "大单净流入多反而跑输"
  在 5.5 年长样本上稳健, 说明市场微观结构里 informed selling 伪装成大单买入普遍存在
- **合成不总能增强**: 当两个因子 mechanism 相似 (都是 smart/dumb money divergence),
  叠加反而分散 (MFD 弱信号拖累强 RIAD)
- **northbound 数据在 2024-2025 严重稀疏** (每股每年几乎只有几十行), 需要重新下载或换源

### 下一步 (待 jialong 决)

Option A (推 RIAD): 行业中性化 + 融券成本假设 + long-only 做多 Q1 + walk-forward + DSR #32
Option B (强化 MFD): 只在 retail-dense 股票里跑, 或改短周期 (5d) 捕捉拉高出货 T+2 反转
Option C (其他数据): broker_recommend 金股 fade / suspend+limit 复牌抛压 / pledge 强平

详情见 `journal/riad_mfd_factor_result_20260422.md`.

### 红线

- 不基于 IS 调整 RIAD 参数 (window=20/60, 采样 5d 已锁)
- 样本外必须覆盖 2026 年 (当前仅到 2025-12)
- Option A 进 paper-trade 前必须过 DSR 门 (CI_low ≥ 0.5)
